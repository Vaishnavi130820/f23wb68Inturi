extends layout
block content
  h1= title
  - var items = results;
  div(class="container-fluid")
   div(class="row")
    each v,index  in items 
     div(class="col-sm-2" onclick =`buy(${index})`) #{v.animalName}, #{v.animalAge}, #{v.animalCost}
      script. 
        function buy(a){
            alert(`You purchased Item ${a+1}`)
        }